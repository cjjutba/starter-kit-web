# Progress

Where the build stands. Read it first in every session, before any other
doc. The skills keep it current: `/setup` writes the first state, `/plan`
lists the features, `/feature` moves one through building to shipped, and
`/intake` adds what is still open. Why something was decided belongs in
`product/decisions.md`, not here.

## Phase

Template. `/setup` has not run.

## Now

Nothing in progress. When a feature is being built: its id, its branch and
the step of `/feature` it is on.

## Features

| Id | Feature | Status | Spec |
| --- | --- | --- | --- |
| F0 | Foundation, shipped by starter-kit-web | shipped | none, see `product/features.md` |

Status is `planned`, `building` or `shipped`. A feature is shipped when it
is on production and green, not when it merges.

## Next

`/setup`, then `/plan`.

## Open questions

None yet. Name the skill or feature that is blocked on each.

## Resume notes

What the next session needs that the repo does not already say: a half
finished step, a command that failed and why, a person waiting on an
answer. Clear a note once it no longer applies.
