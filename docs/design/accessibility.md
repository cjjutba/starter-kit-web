# Accessibility

The record, not the rules. The rules are in `DESIGN.md`.

## What is checked

axe with the WCAG 2A and 2AA tags on every public route in `src/content/routes.ts`, in light and dark, on every CI run. Keyboard focus by hand on every new screen. Reduced motion by toggling the OS setting.

## Contrast

The token pairs and their ratios are in `DESIGN.md`. Add product tokens here with their ratios when they are added.

## Audit record

| Date | What | Result | Fixed |
| --- | --- | --- | --- |

## Known gaps

Anything that fails and why it is accepted for now. Empty is the goal.
